<UserControl x:Class="GearGauge.UI.Controls.SpinEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <Style TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="BtnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{DynamicResource HoverNavigationBackgroundBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{DynamicResource SelectedNavigationBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border x:Name="MainBorder"
                Background="{DynamicResource InputBackgroundBrush}"
                BorderBrush="{DynamicResource InputBorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Height="34"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="30" />
                </Grid.ColumnDefinitions>

                <!-- Text input area -->
                <TextBox x:Name="ValueInput"
                         Grid.Column="0"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         CaretBrush="{DynamicResource AccentBrush}"
                         Padding="10,0"
                         VerticalAlignment="Center"
                         VerticalContentAlignment="Center"
                         FontSize="13"
                         TextAlignment="Center"
                         Text="{Binding ValueText, ElementName=Root, UpdateSourceTrigger=LostFocus}"
                         GotFocus="OnInputGotFocus"
                         LostFocus="OnInputLostFocus"
                         PreviewTextInput="OnPreviewTextInput" />

                <!-- Separator -->
                <Border Grid.Column="1" Background="{DynamicResource InputBorderBrush}" Margin="0,6" />

                <!-- Up / Down buttons -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="1" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <RepeatButton x:Name="UpButton" Grid.Row="0"
                                  Click="OnUpClick"
                                  Interval="150"
                                  Content="&#xE70E;"
                                  FontFamily="Segoe Fluent Icons"
                                  FontSize="8" />
                    <Border Grid.Row="1" Background="{DynamicResource InputBorderBrush}" />
                    <RepeatButton x:Name="DownButton" Grid.Row="2"
                                  Click="OnDownClick"
                                  Interval="150"
                                  Content="&#xE70D;"
                                  FontFamily="Segoe Fluent Icons"
                                  FontSize="8" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
